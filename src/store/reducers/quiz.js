const initialStore = {
    quizes: [],
    loading: true
}

export default function quizReducer(state = initialStore,action){
    switch (action.type) {
       default:
           return state
    }
}